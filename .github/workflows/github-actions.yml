
name: Github Drowned Moses
on: push
jobs:
  Update-Application:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout repo
        uses: actions/checkout@v2
      -
        name: Build Moses container
        run: cat ${{ github.workspace }}/README.md
